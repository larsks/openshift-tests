apiVersion: v1
kind: Pod
metadata:
  name: test-pod-{{testid}}
  labels:
{% filter indent(4) %}
{% include "common_labels.yaml" %}
{% endfilter %}
spec:
  containers:
  - image: "{{ testimage }}"
    imagePullPolicy: Always
    name: test-container
